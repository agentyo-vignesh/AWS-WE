name: my-deployment

on:
  push:
    branches: [ main ]
    workflow_dispatch: {}

jobs:
  build:
    runs-on: self-hosted
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

  deploy:
    needs: build
    runs-on: self-hosted
    steps:
      - name: vignehs
        run: |
          echo "Deploying to Ngihhhnx..."
          # Add your deployment commands here, e.g., copying files, restarting services, etc.
          # Example:
          whoami
          scp -r ./index.html /var/www/html/index.nginx-debian.html
